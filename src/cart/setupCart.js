// import
import { setStorageItem, formatPrice, getElement } from '../utils.js'
import { openCart } from './toggleCart.js'

// set items
export const addToCart = (id) => {
  console.log(id)
  openCart()
}
